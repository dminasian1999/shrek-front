

const Store = () => {
    // const {products, setProducts, selectedId, setSelectedId} = useContext(ProductsContext);
    //
    // const id= useLocation().pathname.split('/')[2]
    //
    // const selectedCategory = categories.find((c) => c.title === selectedId) || categories[0];
    //
    //
    // const getImageUrl = (img: string) => img?.startsWith('http') ? img : `${baseUrlBlog}/${img}`
    //
    // // Fetch products on mount
    //
    // // useEffect(() => {
    // //     if (id) {
    // //         const prod = products.map(p => p.category === id)
    // //         setProducts(prod)
    // //     }
    // // }, [id])
    return (
    <div className={'app-body'}>
    </div>
    );
};

export default Store;
